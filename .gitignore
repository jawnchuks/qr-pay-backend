# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Compilation
dist/
*.tsbuildinfo

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS
.DS_Store

# Prisma
# /prisma/migrations/
# Usually we want migrations, but if the user wants a clean repo, maybe they don't?
# Actually, the user's PRD mentioned wanting to be "free from errors".
# Let's include migrations as they are part of the source of truth for the DB schema.
# So I will NOT ignore them.

# Docker
docker-compose.override.yml
